import 'package:flutter/material.dart';
import 'package:appmobi/proj/login.dart';

void main() => runApp(
  const MaterialApp(
        home: Login(),
        debugShowCheckedModeBanner: false,
      ),
  );
  
